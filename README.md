# RBX-Fileview

Human-readable, diff-friendly dumper for Roblox place and model files.

RBX-Fileview deserializes `.rbxl`, `.rbxlx`, `.rbxm`, and `.rbxmx` files using [Lune](https://lune-org.github.io/) and prints a YAML instance tree